# libc

TODO

the `libc` module is mostly used internally by other modules like `DumpELF` or `dog fetch`, so there are no public-facing functions, but there's probably still some stuff worth mentioning.
