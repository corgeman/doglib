[build-system]
requires      = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name            = "doglib-rs"
requires-python = ">=3.12"
dynamic         = ["version"]

[tool.maturin]
module-name = "doglib_rs"
features    = ["pyo3/extension-module"]
