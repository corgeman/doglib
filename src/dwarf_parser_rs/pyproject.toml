[build-system]
requires      = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name            = "doglib-dwarf-parser"
requires-python = ">=3.8"
dynamic         = ["version"]

[tool.maturin]
module-name = "doglib_dwarf_parser"
features    = ["pyo3/extension-module"]
